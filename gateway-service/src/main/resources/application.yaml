spring:
  application:
    name: gateway-service
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
route-management:
  base-url: http://localhost:8085
